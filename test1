-- Script Tool Giver para Natural Disaster Survival (Roblox)
-- Certifique-se de que este script esteja no seu jogo como um objeto de serviço (por exemplo, um objeto de script).

local ToolGiver = {} -- Tabela para armazenar os IDs dos itens e suas funções de entrega

-- Adicione os IDs dos itens e suas funções de entrega aqui
ToolGiver[125013769] = function(player) -- Exemplo: ID do item é 123456
    local item = Instance.new("Tool") -- Crie uma nova ferramenta
    item.Name = "Meu Item" -- Nome do item
    item.Parent = player.Backpack -- Coloque o item na mochila do jogador
end

-- Evento para quando o jogador insere o ID do item
game.Players.PlayerAdded:Connect(function(player)
    player.Chatted:Connect(function(message)
        local itemID = tonumber(message) -- Converta a mensagem do jogador para um número
        if ToolGiver[itemID] then
            ToolGiveritemID -- Entregue o item correspondente
        else
            player:Kick("Item não encontrado ou ID inválido.") -- Mensagem de erro se o ID não for válido
        end
    end)
end)
